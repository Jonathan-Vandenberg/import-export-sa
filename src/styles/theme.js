import { createTheme } from '@mui/material/styles'
import { green, purple, eggShellWhite, black } from '@mui/material/colors'

export default createTheme({
  palette: {
    primary: {
      main: '#e3f2fd'
    },
    secondary: {
      main: '#ggg'
    },
    background: {
      paper: '#e3f2fd'
    }
  }
})
